export enum HTTP_METHOD {
  GET = "GET",
  POST = "POST",
  PUT = "PUT",
  UPDATE = "UPDATE",
  DELETE = "DELETE",
}

export enum COLUMN {
  NAME = "name",
  TYPE = "type",
  DESCRIPTION = "description",
}

export enum ORDER {
  ASC = 1,
  DESC = -1,
}
