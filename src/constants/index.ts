export enum METHODS {
  GET = "GET",
  POST = "POST",
  PUT = "PUT",
  UPDATE = "UPDATE",
  DELETE = "DELETE",
}

export enum COLUMNS {
  NAME = "name",
  TYPE = "type",
  DESCRIPTION = "description",
}

export enum ORDERS {
  ASC = 1,
  DESC = -1,
}
